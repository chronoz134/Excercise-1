#include <iostream>

int main() 
{
 		//variable initialization and declaration with values provided from pompt
  	int firstnum = 50;
  	int secondnum = 100;
  
  	//we declare total and give it the sum of the first two variables.
  	int total = firstnum + secondnum;

  	//printing out the sum of the first two vars that was placed into total
  	std::cout << total;
}